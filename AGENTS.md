# AGENTS.md

> 本文件是本仓库（dsh-auto-pass）的活文档，只记录已验证的事实；改动代码后请同步更新对应小节。

## 项目概览

- **定位与出处（2026-09-16）**：本项目只解决「自动审批通过」（审查通过的动作自动放行），**不解决无人值守问题**——该转人工的仍然转人工、由用户拍板；本项目 fork / 参考自 <https://github.com/simon300000/dsh-auto/>。两个 README 的开头都要以醒目的引用块写明这两点。
- `dsh-auto-pass`：给 DSH WebUI 增加 `自动审批` 权限档位（显示名在 `cordis.patch.yml` 的 `presets` 表里，预设 **id 固定为 `auto-approve`**，它是插件闸门）。入口 `src/index.js`，在 `approval/request` waterfall 上注册应答器。
- **核心语义**：插件自动给出的结论有两种——`allowed-once`（只自动放行 Reviewer 判定 `outcome: allow` 的请求）与「设置页 `denyDirect` 打开时命中黑名单的 `rejected`」；其余情况（模型 deny、宿主安全降级 critical / high 授权不足、审查失败：超时 / 无审查路由 / 拿不到精确动作 / 模型调用失败或输出非法）一律调用 `next()` 转回 DSH 原生人工审批链（ask），由用户决定。**默认 `denyDirect: false`，即黑名单也只转人工**。
- 审查提示词在 `prompts/review.md`（极简判定清单 + 一行 JSON 输出契约），规则优化提示词在 `prompts/rule.md`；旧的 `policy.md` / `policy-template.md` / `rule-template.md` 已删除。
- **权限记忆（`src/policy.js`）**：白名单（直接放行）/黑名单（直接转人工）分项目与全局两级，命中名单**不启动 Reviewer**；同一项目同一签名连续放行（默认 3 次；**计数以最终结果为准**——插件自动放行与你点「允许一次」都算放行，哪怕模型原本判了 deny）或连续被拒（默认 3 次；最终未被批准才算，含你拒绝与「模型判 deny 且无人放行」）达到阈值后，**用「本次审查回复里的建议规则」，没有可用的建议就用 `defaultRuleOf`（有命令＝命令前缀，没有命令才用精确签名）作为匹配条件，交给 `ctx.userQuestions.ask` 问用户是否加入名单**——用户确认才落盘，**不再静默升级**，也不再为固化单起一次模型调用。匹配器只认三种闭集条件：`signature`（精确签名）/ `command_prefix`（命令前缀）/ `path_prefix`（路径前缀，支持**单层** `*` 通配，`**` 明确拒绝）；时间线上的手动升级/降级仍优先采用 Reviewer 的 `rule` 建议。

## 命令（已验证）

- 安装依赖：`pnpm install`（Node ≥ 22.19；`~/.npmrc` 的 registry 为 npmmirror，装 vitest 约 2 秒）。
- 跑测试：`pnpm test`（= `vitest run`，当前 183 个用例全绿：`auto-approve` 42 + `records` 17 + `package` 4 + `policy` 64 + `policy-gate` 36 + `client` 20）。
- **测试进程的 `DSH_HOME` 由 `tests/setup-env.js` 指向临时目录**（`vitest.config.js` 的 `setupFiles`）：本机环境变量 `DSH_HOME=C:\Users\czy\.dsh` 就是真家目录，不隔离的话 `apply()` 不带 `logFile`/`policyFile` 的用例会直接读写用户真实数据（2026-09-15 实测踩到：真实 `approvals.json` 被拆了一次、真实 `policy.json` 的计数键被折算）。`test.env` 这条配置在 vitest 4 的 worker 里**不生效**（实测），别用它做隔离。
- **客户端半只有 `tests/client.spec.js` 覆盖**：它不进构建流水线。测试用假 `window.__ModuleLoader__` + 带「渲染帧」的假 react 加载 bundle，把组件**渲染到稳定状态**（反复求值 + 跑副作用 + 等微任务，直到没有新的 `setState`）——漏定义变量、漏闭合花括号、以及「异步拉到记录之后」那一轮渲染里的问题就靠它兜住（都真的漏过）。
- `tests/auto-approve.spec.js` 文件名沿用权限档位名 `auto-approve`，与包名 `dsh-auto-pass` 不同，改名时不要误删。
- **受限沙箱下 `pnpm test` 会 `spawn EPERM`**（vite 会 `exec("net use")`、vitest 默认 forks 池也要 spawn 子进程）；需要以更宽权限运行，否则测试跑不起来。

## 架构事实（读源码确认）

- `ctx.on('approval/request', handler, { prepend: true })`（`src/index.js` 的 `apply`）：靠 `prepend` 抢在 `@deepseek-ai/dsh-api-remotes`（把审批转发给 WebUI 人工应答）之前，**不依赖 bundles 顺序**。
- 审批 waterfall 的返回值只能取自 `'allowed-once' | 'rejected' | 'cancelled' | 'unavailable'`（`@deepseek-ai/dsh-user-approval` 的 `OUTCOMES`，非闭集值会被归一化为 `unavailable`）。**没有 `'ask'` 这个返回值**：所谓「转 ask」= 不返回结论、调用 `next()` 让后续应答器接手。
- `ApprovalService.decide()` 先看会话 `approval/policy`：为 `never` 时直接返回 `'rejected'` 且**不分发 waterfall**，因此本插件只在 policy ≠ never 时才会被调用。
- **审查是一次单轮模型调用，不起子代理（2026-09-15 按用户要求重做，省 token）**：`callModelOnce()` 走 `ctx.get('llm').stream({ provider, model, messages, system, maxTokens, reasoningEffort, sessionId, purpose, signal })`，读一次回复就结束；system 是 `prompts/review.md`，user 消息由 `buildReviewPrompt()` 组成两段 JSON：① 归一化后的动作（签名 `text`/`command`/`paths`/`cwd`/提权标记）② 极简证据（`buildEvidence()`：最后一条**直接用户消息** + 最近一次 `ask_user_question` 回答，各自截断到 `maxEvidenceChars`，默认 400 字）。回复要求一行 JSON，`parseJsonReply()` 取第一个 `{...}`（容忍 ```json 围栏），再走原有的 `parseAssessment()` + `enforceHostPolicy()`。没有 llm 服务 / 没有路由 / 解析失败 / 超时一律转人工（fail-closed）。`@deepseek-ai/dsh-llm` 的 `BlockAssembler`/`createUserMessage` 用动态 import 引入（软依赖），测试里用 `vi.mock` 顶掉。
  - 随之删掉：子 Agent 隔离（`agent/created` 里钉只读沙箱/`approval=never`）、工具 guard、`agent/pre-step` 步数上限、`structured_output` 通道、`subagents.start` 调用与转录/调查预算那几个配置键；`inject` 从 `['approval','subagents','tools']` 收窄成 `['approval']`。
  - token 账：旧路子每次审查都要新建一个子 Agent（system 上限 6k + 消息转录 4k + 工具转录 3k）；现在通常几百 token 进出。
- **ptc 档位现在只影响「取参数」与「取证据」**：`exactAction` 在审批请求带派生 id `<父 callId>:ptc:<n>` 时回退到 `tool/ptc-dispatch-start`（会话里没有该 id 的 `tool/call`），`buildEvidence` 也认 `tool/ptc-dispatch` 的内层结果（内层 `ask_user_question` 的回答仍算授权证据）。既然不再给 Reviewer 任何工具，早期那套「ptc 档位必须放行 run_code、否则 Reviewer 交不了结论」的坑不复存在。
- 排查材料：客户端信标 `GET /api/dsh-auto-pass/beacon?stage=&detail=` 把 boot 阶段写进宿主日志；宿主日志 `%APPDATA%\DSH Desktop\logs\host\dsh-<date>.log` 里能看到每次审查的 `全部完成 / 未完成并转人工审批` 记录（单轮调用只有一个 request，排查比子代理时代简单）。旧版本留下的 Reviewer 子会话仍在 `~/.dsh/sessions/<workspace>/<sessionId>/session.v3.jsonl.zstd`。
- 通知通过 `agent.inject({ source: { kind: 'plugin', plugin: 'dsh-auto-pass', form: 'notice' } })` 写进父 session（**由设置页 `notice` 开关控制，默认开**；关掉后审批结果不注入，模型看不到审批发生过——**唯一例外是「人工拒绝理由」**，它只受 `askRejectReason` 控制，见下条）。**注入时机在审批结论确定之后**：调用链是 `finish(outcome, decision, notice)` → `await settled` → `injectReviewNotice(ctx, request, { notice, settled, decision, language })`，所以转人工的请求能写出**最终结果**（人工批准 / 人工拒绝 / 无人应答），不再是审批那一刻的猜测。**正文仍然只有一行**（会进模型上下文），形如 `[标签] 自动审批 已自动批准 bash · 命中：… · low/high · 0 步 · 最终结果：已批准 · 理由：…`（结论三选一：`已自动批准` / `未自动批准 …，已转交你审批` / `已直接拒绝 …（命中黑名单）`；这里的「N 步」是**审查调查步数**，单轮审查恒为 0，与下面时间线显示的「第几轮第几步」不是一回事），上限 `MAX_NOTICE_LINE_CHARS = 240`；**折叠标题（`source.summary`）用同一个标签 + 状态词**（命中名单时是 `[白名单·自动]` / `[黑名单·人工]`，否则 `[自动]` / `[人工]`，后接 `自动审批：已批准` / `已转人工审批` / `已直接拒绝`）——用户要求正文与标题一致。Reviewer 会话、建议规则、命中规则全文都不进上下文（留在审批时间线与宿主日志）。
- `maxConsecutiveDenials` 与 turn 中断逻辑已删除：拒绝不再阻断，交给用户后可继续审批。
- **转人工时把审查意见写进审批请求的 `reason`（2026-09-15 新增）**：DSH 人工审批卡的首行渲染的就是 `req.reason`（`dsh-client-ui-approval` 的 `headline = pending.reason ?? 「工具 X 请求越权执行」`）；`reason` 由 waterfall 的**同一个对象引用**经 `dsh-api-remotes` 转发给浏览器，而转发队列要到插件的 `next()` 之后才被消费并序列化，所以插件在 `next()` 之前就地改写一定生效。`attachReviewNote()`（`src/index.js`，意见上限 `MAX_APPROVAL_NOTE_CHARS = 200`）把意见拼成**另起一段**：`<调用方原文>\n\n<标签><意见>`（标签默认 `自动审批：`，模型真给出结论的那条路径用 `模型审批意见：`；调用方没给 reason 时只写标签+意见那段），接在全部 5 个转人工落点：`deferWithoutReview()`（action 拿不到 / 过长 / 无路由）、黑名单命中、模型结论非 allow（含审查失败）。自动放行与非 `auto-approve` 档位**不改** reason。注意 `approval/asked` 会话事件里的 reason 是 `ApprovalService.request()` 在 `decide()` **之前**写的原文，不受影响——所以刷新页面若从事件重建卡片会退回原文（未验证的边角）。**2026-09-15 15:42 真机验证通过**（重启后）：审批卡首行与记录 `reason` 一致（当时版式是 `[自动审批] <模型意见>｜escalate sandbox …`），**2026-09-15 15:50 新版式真机验证通过**（客户端重启后：卡片首行按「原文 / 空行 / 模型审批意见：…」两段显示，记录 `reason` 里正是 `\n\n` 分隔），同一条记录的 `usage` 也落了盘（`{inputTokens:364,outputTokens:109,totalTokens:857,cacheReadTokens:384}`），宿主日志 `审查完成 … tokens=in=364 out=109 cacheRead=384`。
  - **换行靠客户端半注入的 CSS（2026-09-15）**：卡片首行是纯文本 `<div>{pending.reason}</div>`，默认 `white-space` 会把 `\n` 折叠成空格，所以换行必须由渲染端打开——插件客户端启动时在已有的 `#dsh-auto-pass-style` 里加了一条 `[data-approval-key] *{white-space:pre-wrap}`（只认官方卡片稳定的 `data-approval-key` 属性，不依赖其哈希类名）。**传 HTML 行不通**：`pending.reason` 是 React 的 children，字符串一律渲染成文本节点（`<span>` 会原样显示成源码），宿主也没用 `dangerouslySetInnerHTML`；要「分段着色」只能改宿主 `dsh-client-ui-approval` 的 JSX（`C:\Program Files\...`，升级会覆盖）或让插件接管整张卡片——2026-09-15 评估后选择「不碰宿主、整块同色」。
- **人工审批卡没有任何「填写意见」的入口（2026-09-15 读源码确认）**：`dsh-client-ui-approval/lib/client.js` 的 `ApprovalFlow` 只有两个按钮「拒绝」「允许一次」，点击只调 `pending.answer('rejected' | 'allowed-once')`——**卡片里没有输入框**；协议层也没有承载字段：`ApprovalService.request()` 只落 `approval/asked {id,toolName,callId?,reason?}` + `approval/decided {id,outcome}`，`outcome` 必须属于 4 值闭集（`decide()` 把任何非闭集返回值归一化成 `unavailable`），v0 会话格式的冻结 schema 同样是 `disposition(['id', 'outcome'])`（`dsh-session-format-v0-to-v1`）。所以「人工填理由回传」在原生里**不存在**，不是插件没读。人工决定能进模型上下文的只有**间接证据**：`dsh-sandbox` 的 `approveEscalation` 在 `rejected` 时抛 `the user rejected escalating this <subject> to "<mode>"` → 工具失败结果（`tool/result`，surface 事件）→ 模型看得到「被拒绝了」但看不到理由；`approval/asked` / `approval/decided` **不是 surface 事件**，全局搜索没有任何模型上下文消费者（只有会话格式校验、invariant、客户端投影）。想让模型看到人的文字，现实路径只有三条：① 用户在对话里发一条消息（`user/message`）；② 回答 `ask_user_question` 时用「输入你的答案」文本框（`dsh-client-ui-user-questions` 原生支持自由文本，答案进 `tool/result`）；③ 插件在审批结束后用 `userQuestions.ask` 追问一句、再**另注入一行**——**已实现**，见下面的 `askRejectReason`。**想往官方审批卡里塞输入框只有 `conversation.approval.detail` 这一个席位**（`dsh-cordis-client-runner` 的 `CLIENT_SLOT_API` 文档化：kind `single`、ownerProps `{callId}`、declaredBy 客户端审批面板的 composer 条目），但它已被 `dsh-client-ui-chat` 的 `ApprovalCommand` 占用，注册进去 = `replaceRisk: "shadows-shipped-ui"`（命令详情被顶掉），且输入框的值**无法**随 `answer(outcome)` 回传（只认闭集字符串），只能走插件自己的 HTTP 路由 + 时序妥协。
- **审批记录里的 token 用量（2026-09-15 新增）**：`callModelOnce()` 返回 `{ text, usage }`（usage 取自 stream 的 `usage` 块，即 `BlockAssembler.usage`；`normalizeUsage()` 只保留 `USAGE_FIELDS` 里的有限数字）。审查成功与「模型回了内容但不合法、token 已经烧掉」两条路径都把用量放进 `decision.usage` → 记录的 `usage` 字段；提供方不给用量时该字段**不存在**（老记录也没有）。宿主日志的「审查完成 / 审查未完成并转人工审批」行加了 `tokens=in=… out=… cacheRead=…`（`describeUsage()`）；时间线详情多一行「Token 消耗」（客户端 `formatUsage()`，只显示记录里有的字段）。规则优化那次调用（`optimizeRule`）只写日志、不进记录。
- **审批前的判定顺序（`createAutoApprovalHandler`）**：`action === undefined` → 直接转人工（且**不建立签名**）；否则先查名单，`deny` 命中按设置分两条路（见下条）、`allow` 命中直接 `allowed-once`（都不建 Reviewer）；都没命中才走模型审查；`tests/policy-gate.spec.js` 用「`ctx.llmCalls` 长度为 0」把这一点钉死，并要求记录里带上 `policy.{list,scope,label}`（时间线据此显示命中名单）。
- **`denyDirect`：命中黑名单直接拒绝（设置页开关，默认关）**：`createAutoApprovalHandler` 开头读一次（`effectiveDenyDirect` ← 设置命名空间 `denyDirect` ← config ← 默认 false）。开启时黑名单命中**不调用 `next()`**，直接 `finish('rejected', { verdict: 'blacklist-reject', policyHit }, { outcome: 'defer', rejected: true, … })`：审批卡不弹、工具调用被判为拒绝；记录里 `verdict='blacklist-reject'` / `outcome='rejected'` 且照旧带 `policy`（时间线显示「直接拒绝」徽标 + 黑名单标签），`reason` 仍写上理由（日志与记录都看得出为什么拒绝）。插件自己决定的拒绝**不参与权限计数**（`decision.policyHit !== undefined` 时 `observe` 不吃信号，与白名单命中同一条规则）。默认关 = 保持「直接转人工」。
- **达阈值后的升级建议（`observeDecision` → `proposeRule`）**：`policy.observe` 只返回 `{approvals, denials, suggestion}`，**不再自行落规则**；`finish()` 拿到 `suggestion` 后立刻 `void proposeRule(...)`——**旁路异步**执行，既不改变已经确定的审批结论，也不阻塞这次工具调用（同一签名同一名单同时只挂一个问题）。proposeRule 先取规则：本次审查回复里顺带给的 `suggestedRule`，没有可用的建议就用 `defaultRuleOf(signature)`——**有命令给 `command_prefix`，没有命令才退回 `exactRuleOf` 的精确签名**（用户 2026-09-15 要求「模型生成规则默认使用命令前缀」；**不再为「固化」单起一次模型调用**）；再 `ctx.userQuestions.ask({ questions, agent })` 问「加入白名单/黑名单（本项目/全局）/ 不加入」；同意则 `addRule(rule)` 并回写记录的 `ruleApplied`，选「不加入」则 `dismiss`（该签名该名单从此不再计数、不再询问）。
- **ask 的三个前提与失败姿态**：① `ctx.get(userQuestions)` 不存在（没人应答）→ 只记日志、不写规则；② `agent` 必须是在册的 runtime root，子 Agent 的审批会抛 `DELEGATED_CALLER` → 同样只记日志（触发时计数已清零，下次再攒够阈值会重问）；③ 回答按我们自己生成的选项 label 精确匹配，认不出来一律按「不加入」处理。三者都只影响「规则有没有被写入」，绝不影响审批结论。
- **记忆键 ≠ 精确签名（2026-09-15 在真实记录里实测到的坑）**：`signatureOf` 现在同时给出 `key`（精确签名，规则匹配与时间线手动升级用它）与 `memoryKey`（同样的算法，但先抹掉 `NOISE_ARG_KEYS` = `description` / `justification` / `timeoutMs` / `timeout_ms` 这些只影响展示与执行管道、不改变「在授权什么」的参数）。原因是查 `~/.dsh/dsh-auto-pass/approvals.json` 发现：同一条 `pnpm test` 的两次自动放行因为 justification 与 description 逐次不同而拿到**不同的精确签名**，连续计数永远攒不到阈值——记忆功能在真实使用里等于死代码。计数与 `dismiss` 一律用 `memoryKey`，规则匹配仍用 `key`，因此计数变粗**不会放宽任何规则**；提权标记（`sandbox_permissions`）不在噪声表里，提权重试仍与普通调用分开计数。
  **命令侧的噪声还有「管道之后」（2026-09-15 第二次实测补上）**：`countingCommand()` 砍掉第一个**不在引号内**的 `|` 及其后面整段（引号内的竖线常见于正则，用 `firstPipeOutsideQuotes()` 跳过），因为管道后面只决定「怎么显示输出」。踩坑现场：用户连续人工放行了 4 次 `pnpm vitest run tests/client.spec.js 2>&1 | Select-Object -Last 60/45/30/26`，每次都是一条新的计数键（各 1 次），阈值 3 永远攒不到。**只影响计数键**：精确签名与规则匹配仍然逐字比对。
  **计数键的第三轮归一化（2026-09-15 晚，用户反馈「命令稍微不一样就判定失败」后落地）**：① 结尾的纯输出重定向与管道之后一样抹掉（`countingCommand` 认 `2>&1` / `>nul` / `2>/dev/null`；真会写文件的 `> out.txt` 不抹）；② `workdir`/`cwd` **不进计数键**——计数键本身已经带 `<cwd>` 前缀，同一个工作区里的目录写法差异（写没写、`D:\x` 还是 `D:/x`）只该算同一条（实测 6 组被拆开）；精确签名里仍然保留，但按 `normalizePath` 归一化（只归一分隔符，仍区分不同目录）；③ `signatureOf` 是**纯函数**，绝不拿会话 cwd 去补缺省的 workdir——否则同一组参数会因为调用点知不知道 cwd 而得到不同签名（第一版就这么写，被 `tests/policy-gate.spec.js` 的黑名单用例抓住）；④ 老形状的键（管道之后、未归一化 workdir、结尾重定向）在当前算法下再也产不出来：`canonicalMemoryKey()` 把键折算成当前形状（幂等），`canonicalizeCounters()` 在 `createPolicyStore` 启动时跑一次并写回文件，同一目标键取**较大值**（不求和，免得凭空攒出连续次数），折算条数经 `options.info` 记一行日志（本机实测 23 键 → 13 键）。命中名单的那次按设计不计数（白名单命中的 `pnpm test` 记录 `approvals=0` 属正常）。
- **计数信号以「最终结果」为准（2026-09-15 修正，真机踩到）**：`decisionSignal(settled, decision)` 现在先看 settled——**最终获准执行就是「连续放行」**（插件自己放行的、以及你在审批卡上点「允许一次」的都算），`rejected` 算「连续被拒」，只有**没人拍板**（`unavailable` / `cancelled`）时才回落到「模型判过 deny 就算一次未获批准」。旧实现把 `verdict === 'deny'` 放在最前面，于是「模型判 deny → 转人工 → 你点允许一次」被计成**连续被拒**（与函数自身注释相反），用户越批准越在攒黑名单计数、白名单永远攒不到阈值——真机记录里那批 `deny/allowed-once/human` 记录全是 `denials=1`。测试：`tests/policy-gate.spec.js` 的 `decisionSignal` 用例 + 「模型判 deny、你连续放行三次 → 问的是白名单」端到端用例。
- **两侧计数与阈值**：计数键是裸 `<memoryKey>`（老全局形态曾是 `<cwd>` + NUL + `<memoryKey>`，迁移时按前缀拆到各工作区文件），同一条目里 allow / deny 两侧各自累计，相反信号把另一侧清零；阈值按名单分开存（`thresholds.allow` / `thresholds.deny`，HTTP `op=threshold` 带 `list`），旧文件里的单个 `threshold` 字段按白名单阈值兼容读取。**达到阈值时该键清零**；用户选「不加入」则写成 `{"allow":0,"deny":0,"dismissed":{"deny":true}}`（2026-09-15 真机：`thresholds.deny=2` 被两次拒绝触发询问、选「不加入」后的磁盘形状），此后该签名该名单不再计数、不再询问。
- **计数不再无限膨胀，且按工作区拆成独立文件（2026-09-16：用户先问上限，随后明确要求「还是想拆」）**：计数键里含参数 JSON（真机实测：平均 132 字符、每条目约 200 字节），原先**既无上限也无清理**，而每次审批都会把整份策略文件重写一遍 —— 长尾只增不减（真机文件 5 条计数里 3 条是「双侧 0 且未 dismissed」的死条目）。现在：① 条目多写一个 `at`（最后更新时刻；`counterValue` 保留、`mergeCounterEntries` 取较大值，老条目缺 `at` 视作 0＝最旧）；② `pruneCounters(counters, { max, protect })` 是**单工作区口径**（一个文件就是一个工作区，不再按 `\u0000` 前缀分组）：删掉死条目并压到 `DEFAULT_MAX_COUNTERS = 500` 条以内，淘汰顺序「活动条目按 `at` 升序 → `dismissed` 条目最后」（dismissed 是用户「别再问我」的明确意图），`protect` 保证**正在更新的那一条永不淘汰**；③ **存储拆分**：计数落在 `$DSH_HOME/dsh-auto-pass/counters/<slug>.json`（`defaultCounterDir()` + `records.js` 的 `workspaceSlug`，没有 cwd 进 `unknown.json`；文件形状 `{version: COUNTER_FILE_VERSION, counters: {<裸 memoryKey>: {allow,deny,dismissed?,at?}}}`），`observe` / `dismiss` 只重写**本工作区**那一个文件，全局策略文件从此只放阈值与全局名单（`emptyDoc` 的 `counters` 只在读老文件时出现，迁走后就 `delete`）；④ 启动顺序：`canonicalizeCounters` 折算老键 → 按 `<cwd>` 前缀分组 → 合并写进各工作区文件（同键取较大值，用 `mergeCounterEntries`）→ **全部写成功才**从全局文件删掉 `counters` 并记一行 `已把 N 个计数键按工作区拆进 M 个计数文件（清掉 X 个零值/超限条目）`；任何一个文件写失败就整体放弃、老数据留在全局文件里下次再试 → 再对计数目录里已有文件逐个 `canonicalizeMemoryCounters` + prune（有变化才写回，记一行 `已整理 N 个计数文件`）；⑤ `createPolicyStore({ globalFile, counterDir })` 两个目录都可换（测试用），返回对象多一个 `counterDir`；`index.js` 启动日志多一段 `counters=<dir>`。测试：`tests/policy.spec.js` 的 `pruneCounters` 纯函数 5 例、`canonicalizeMemoryCounters` 1 例、store 级 6 例（迁移拆分并删掉全局 `counters` / 启动整理计数文件 / observe 只写本工作区文件且不创建策略文件 / 不同工作区互不牵连 / 超限淘汰 / dismissed 更晚淘汰）。
- **签名的三条安全性质（防过的坑）**：① `action` 拿不到时不建签名——否则所有解析不出参数的请求塌缩成同一个空签名，会被一起计数、一起触发询问；② 命中名单的那一次不计数（`decision.policyHit !== undefined`）：用户刚把这类动作写成规则，这一次的放行/拒绝是**单次**决定，不该继续滚成记忆；③ 达到阈值只产生**建议**：规则一律经「模型建议（本次审查顺带给出），没有可用建议就用 `defaultRuleOf`（命令前缀／精确签名）→ 用户确认」才落盘，选过「不加入」的动作记进 `dismissed` 不再询问。黑名单永远压过白名单。
- **不能给会话写「模型看不到的自有事件」（2026-09-15 实测）**：`dsh-session-persistence` 的 `assertStoredEvents` 在读取持久化会话时，遇到未知事件类型且没有 `ignorable: true` 就抛错**拒绝加载整个会话**；而 `session.append()` 只写 `type/seq/time/data/surfaceOp/sourceEventSeqs`，**没有任何途径设置 `ignorable`**（写入器 `dsh-session-log-deepseek` 的 `wireEvent` 也只是透传）。所以用自有事件类型（例如 `dsh-auto-pass/approval`）写进会话 = 重启后该会话打不开。另一个硬事实：模型可见的 surface 只有 4 种事件（`system/message`、`user/message`、`assistant/message`、`tool/result`，且 `tool/result` 强制带 surfaceOp），因此「在对话气泡里可见」与「不进模型上下文」**不可兼得**——审批卡/todo/plan 这些模型看不到的原生事件只出现在「轨迹」视图里。结论：审批记录要么**压成一行注入**（模型可见但极短，当前选择），要么只留在插件时间线与宿主日志。
- `signatureOf` 会把字符串形态的 `arguments` 解析一层：`tool/call` 事件里 `arguments` 时而对象时而 JSON 字符串，字符串若不解析会退化成空参数签名，等于把一条规则放大到整个工具。
- 策略文件：全局 `$DSH_HOME/dsh-auto-pass/policy.json`（阈值 + 全局名单），项目 `<cwd>/.dsh-auto-pass/policy.json`（项目名单），**连续计数另存** `$DSH_HOME/dsh-auto-pass/counters/<slug>.json`（一个工作区一个文件，与审批记录同一套 `workspaceSlug`）。所以项目目录只在真的写了项目规则时才多出 `.dsh-auto-pass/`。项目写盘失败自动降级写全局；策略 / 计数 IO 失败只告警，绝不影响审批结论。
- **重复添加 = 更新 / 已被覆盖 = 不重复写入（`policy.addRule` 的 `replaced` / `covered` / `merged`）**：`commit()` 的查重是**语义**的，不是字符串相等——
  ① `sameTarget` 比的是「同 `tool` + 同 `match.kind` + 同 `comparableValue`」：前缀类条件折叠空白与大小写（与 `matchRule` 忽略大小写的口径一致），**签名则逐字比对**——签名 key 里含 NUL 与参数 JSON，折叠空白会把 `args:{"content":"a  b"}` 与 `args:{"content":"a b"}` 判成同一条，替换掉旧规则就等于让那条签名失去覆盖。命中就是**更新那一条**并回报 `replaced: true`。
  **实测踩过的坑（2026-09-15，`~/.dsh/dsh-auto-pass/policy.json` + `approvals.json`）**：`pnpm test` 与 `pnpm test `（模型第二次生成时只多了个尾部空格）被判成两条不同规则写进项目白名单，用户只能手动删掉一条——按原始字符串比对永远判不出这种重复。
  ② `ruleCovers(左, 右)` 判「左是否完整覆盖右」，只处理闭集里的确定性情形（判不出来一律 false，宁可多留一条窄规则也不误删）：`signature` vs `signature` 逐字相等；`command_prefix` 覆盖更长的前缀（要求词边界 `PREFIX_BOUNDARY`）或**同一命令的精确签名**（从签名 key 的 `cmd:` 段取命令，非命令类 `args:` 签名不算）；`path_prefix` 覆盖其下目录。→ 已有规则覆盖新规则时**不写新条目**，回报 `covered: true` 且 `rule` 指向那条已有规则；反过来新规则覆盖了更窄的旧规则就**合并掉窄规则**并回报 `merged: N`（同一个名单里永远不出现两条互相覆盖的规则）。
  ③ 比较范围是**同一个（策略文件 × 名单）**：项目与全局、allow 与 deny 之间互不干扰——「本项目已有再加全局」＝放宽到所有项目，是正常操作，不算重复（用户 2026-09-15 选定的口径）。
  三个标志由 `addRule` 带进返回，`/policy` 的 `op=add` 与 `/rule` 两条 HTTP 出口原样透传（`/rule` 另写一行 `规则写入 … replaced=… covered=… merged=…` 宿主日志）；客户端在操作结果里分别提示「（已更新同名规则）」「（已有规则已覆盖这个动作，未重复添加）」「（已合并 N 条被它覆盖的窄规则）」。测试：`tests/policy.spec.js` 的 `ruleCovers` 与查重用例、`tests/policy-gate.spec.js` 的 HTTP 出口断言、`tests/client.spec.js` 的三种文案。
- HTTP：`GET/POST /api/dsh-auto-pass/policy`（快照 / `op=threshold|add|remove|update`）、`POST /api/dsh-auto-pass/rule {recordId, scope, list, sessionId?, rule?}`（时间线一键升级/降级，回写记录的 `ruleApplied`）。`op=update` 按 id 原地改一条已有规则（`policy.updateRule`：id/位置不变、`source` 改记 `user`、新条件与另一条同义或盖住窄规则时照 addRule 的口径合并）。
- **手动升级仍然经过模型，但改成单轮调用（2026-09-15 随单轮审查一起改）**：`/rule` 的规则文本来自——① 记录里已有 `suggestedRule` → 直接用（`optimizedBy: 'record'`）；② 没有就用 `optimizeRule()` 起一次**单轮** `llm.stream`（system 是 `prompts/rule.md`，提示词只带签名文本/命令/路径与目标名单）→ `optimizedBy: 'model'`；③ 没有 llm 服务、没有路由、调用失败或输出不合法 → `ruleFromRecord`／`defaultRuleOf` 的兜底（有命令＝命令前缀，没有命令＝精确签名；`optimizedBy: 'signature'`，客户端文案是「没有可用的模型建议，已回落到默认条件：命令前缀，或没有命令时的精确签名」并用警告色）。**不再需要父 Agent**（旧版要按「客户端 sessionId → 记录 sessionId → 任一在册 root」找 Agent，找不到就没法优化，`liveAgentFor` 已删除）。两条模型路径写下的规则 `source` 都是 `model`。
- **用户可以直接微调规则（2026-09-15 用户要求，解决「模型给的太宽/太窄/干脆是假的」）**：
  ① **加入前可编辑**：时间线记录详情里是一块表单——匹配条件（命令前缀 / 精确签名 / 路径前缀）+ 匹配值 + 规则标签，默认预填这次审查的模型建议、没有可用建议就按 `defaultRuleOf` 预填（有命令＝命令前缀，没有命令＝精确签名）；四个按钮（白/黑 × 本项目/全局）把它们**手填的内容**发给 `/rule` 的 `rule` 字段（`optimizedBy: 'manual'` → 界面显示「已按你填写的条件加入」），宿主校验通过后原样写入，**不再让模型改写**。
    **UI 细节（2026-09-15 用户反馈后定的，别回退）**：匹配条件用**插件自己的分段按钮**（`.ap-seg` + `data-kind`）而不是原生 `<select>`——原生下拉的弹层在深色主题下是白底黑字，样式改不动；**切换条件时匹配值立刻按新条件刷新**（精确签名 → 本次签名 key；命令前缀 → 本次命令砍掉 `|` 之后；路径前缀 → 本次第一个路径；推导不出来就保留原值），不会留下「条件=命令前缀、值却是签名 key」这种自相矛盾的组合，提示语也从「默认来自模型建议」换成「值已按所选条件刷新」；操作区是**竖排**的（`.ap-actions{flex-direction:column}`，规则块一行 → 升级一行 → 降级一行 → 状态一行），之前整排 flex-wrap 会横七竖八。
  ④ **换条件后让模型按该条件重新生成（2026-09-15 用户要求）**：客户端在时间线上切换匹配条件时，① 先本地按条件推导一个值（界面立刻有反馈），② 同时 `POST /api/dsh-auto-pass/rule/draft {recordId, kind, list?, draft?}` —— 宿主走**一次单轮 `optimizeRule`** 并把它写成 `RULE_DRAFT_PATH`，**只生成不落盘**；提示词（`buildRulePrompt`）里带上「用户指定的匹配条件：命令前缀…（match_kind 必须用 command_prefix）」与当前草稿，所以模型知道要产出哪种条件。模型回来时若还停在这个条件上就覆盖草稿（提示「已让模型按所选条件重新生成」），失败或期间又换了条件就保留本地推导值（提示「模型这次没能生成…」）。宿主的验收：模型擅自换 kind、或生成的条件不覆盖本次动作，一律判失败（503）——客户端于是保留自己的本地值。测试在 `tests/policy-gate.spec.js`（提示词含条件、不落盘、两种失败、非法 kind 400）与 `tests/client.spec.js`（请求带条件与本地值、成功覆盖、失败保留）。
  ② **加入后可编辑**：设置面板每条规则多了「编辑」（同一套分段按钮 + 值 + 标签，改条件时值也会刷新：精确签名 key 里含 `cmd:` 段就把命令取出来当前缀），改完按 `op=update` 原地更新（id 不变，记录里的 `ruleId` 仍指得回来），可以直接把「运行 policy.spec.js 单测」这种窄规则改成 `pnpm test` 这类前缀。
  ⑤ **路径前缀支持单层通配（2026-09-15 用户要求）**：`path_prefix` 的值里允许 `*`，但**只能出现在最后一段**（文件名部分），且**不跨目录**——`D:/repo/src/*.js` 命中 `D:/repo/src/a.js`，不命中 `D:/repo/src/sub/a.js`，也不命中 `D:/repo/src` 本身（那是目录）。`validatePathPattern()` 明确拒绝：`**`（跨目录太宽，等于「任意位置的这类文件都放行」）、`?` 与 `[]`、`*` 出现在中间段、以及 `*.js` 这种**通配符前面没有目录**的相对模式（真实路径都是绝对路径，那种规则一条也命不中）。客户端 `ruleDraftProblem` 有同一套即时校验（写 `**` 时按钮直接禁用并说明原因），选到路径前缀时界面会多一行口径说明。`command_prefix`/`signature` **不认通配符**（命令里的 `*` 是字面量）。用户问过「`pnpm vitest run tests/*` 这样写命令前缀支持吗」——不支持：它只会命中**命令文本里真的带 `tests/*`** 的调用（而且这种值通常过不了覆盖度验收，`/rule` 直接 400）；值里出现 `*` 时界面会补一句提示，想覆盖一类命令就把前缀写短（`pnpm vitest run`）。`ruleCovers` 对通配规则保持保守：目录规则覆盖其下的通配规则（`D:/repo/src` 覆盖 `D:/repo/src/*.js`），通配覆盖通配一律判不出来＝不删。测试在 `tests/policy.spec.js`（命中/不命中、四种非法值）与 `tests/client.spec.js`（提示语 + `**` 禁用按钮）。
  ⑥ **条件必须配得上这次动作（2026-09-15 第三次真机反馈后补上）**：真机上一条 **pwsh 命令**记录（没有文件路径）被切到「路径前缀」，模型给了个目录——旧写法里「`signature.paths` 是空数组 = 判不了 → 信任模型」，于是写进一条**永远命不中任何动作**的规则。现在把「字段缺失」与「字段为空」分开：
  - `signatureFromRecord` **不再伪造空数组**：老记录只有 `toolName/key/text` 时不带 `paths`（表示「不知道」）；新记录一定带 `paths`（可能是 `[]`）。
  - `kindApplicable(signature, kind)`：新记录按实际字段判（没有 command ⇒ 命令前缀不可用；paths 为空 ⇒ 路径前缀不可用），老记录一律按可用。客户端有同口径的同名函数：**这次动作用不上的条件按钮直接禁用**并给一行说明（「路径前缀匹配不到这次动作（它没有对应的命令或路径参数），已禁用」），`chooseKind` 里再兜一层。
  - `suggestionUsable` 同步收紧：新记录里字段为空 ⇒ 那种条件永远命不中 ⇒ 判不可用（丢掉建议、回落到精确签名）；只有**字段缺失的老记录**才「先信模型」。
  - `/rule/draft` 在 kind 与动作不搭时**直接 400 `code='kind-not-applicable'`，连模型都不叫**（省一次调用）。
  - `matchRule` 的 path 分支对「没有 paths 可比」一律返回 false（fail-closed）。
  - **提示词也重写了**（`prompts/rule.md`）：三种 kind 各自的含义与合适的 `match_value`（signature 逐字照抄；command_prefix 只对带命令的动作有意义；path_prefix 讲清单层通配与不跨目录）；`buildRulePrompt` 现在把 `command` 与 `paths` **显式给全**（没有就是 null / 空数组），让模型自己看出哪种条件根本用不上；并要求 label 只描述动作、不写「只读/安全」这类未经证实的判断、路径统一正斜杠。
  ③ **建议有效性校验（`suggestionUsable`）**：模型建议（以及用户手填）的规则**必须覆盖它被建议的那次动作**才可用——精确签名要逐字等于本次签名 key，命令前缀/路径前缀要真的覆盖本次命令/路径；只有**字段缺失的老记录**（连 paths 都没有）才先信模型。真机记录里模型把「精确签名」写成了 `danger-full-access`、`escalation:danger-full-access`、`escalation=danger-full-access` 三种描述，那种规则一个动作都匹配不到却被写进了白名单——现在这种建议一律丢弃并回落到**兜底条件**（`defaultRuleOf`：有命令＝命令前缀，没有命令＝精确签名；`ruleFromRecord` / `chooseRecordRule` / `proposeRule` 三处都过这道闸），时间线上手填同样的假签名会被 `/rule` 用 400 `code='not-covering'` 挡掉。
- 记录里的 `signature` 除了 `toolName/key/memoryKey/text` 还存 `command` 与 `paths`：手动升级时现场那次优化调用需要它们，才能判断前缀该窄到什么程度（老记录没有这两个字段，模型只能靠标签判断）。
- **审查提示词里的规则建议只允许两种条件（2026-09-15 收紧）**：`command_prefix`（有命令时的默认，`match_value` 抄动作里 `command` 的开头一段、去掉 `|` 之后与结尾 `2>&1`）与 `path_prefix`；**明确禁止 `signature`**——`buildReviewPrompt` 只给 `summary`（签名 text）与 `command`，**根本没给签名 key**，模型写的「签名」必然是自创值（真机 10 条建议里 9 条是 `danger-full-access` / `escalation:…` 这类描述）。`prompts/rule.md` 那条路不受影响：`buildRulePrompt` 里有 `signature.key` 原文，`signature` 仍可作为「只想放行这一次」的选项。

## 核验方法（2026-09-15 本机实测）

- **「标准模式 / PTC 模式」是 agent preset，不是 `dsh-tools` 的 `mode: native|ptc|both`**：定义在 `@deepseek-ai/dsh-agent-presets/presets/{standard,ptc,cordis}`（客户端显示的「PTC 模式」「标准模式」文案来自 `dsh-client-ui-agent-preset`）。会话快照 header 里是 `agentPreset: "ptc"`。PTC 会话里模型只能直接调 `run_code`，其余工具经 `tools.xxx()` 走 PTC 派发，所以审批记录的 `callId` 带 `:ptc:<n>` 后缀（`exactAction` 靠它回退到 `tool/ptc-dispatch-start`）；标准模式会话的 callId 没有这个后缀。**header 里的 `agentPreset` 是创建时的值，会话内切换模式不改写它（2026-09-15 实测：`session-0faff011…` 创建时是 `ptc`，用户切成标准模式后 header 仍是 `ptc`，但同一条会话里的审批 callId 已不带 `:ptc:`）——判断当前模式要看 callId 形态，别只看 header。**
- **怎样确认「审查没起子代理」**（三处交叉验证）：① 宿主日志 `开始审查 / 审查完成 … mode=single-shot`，**既无 `reviewerSession=` 也无 `steps=`**（旧版是 `reviewerSession=<id> steps=2`）；② 审批记录**没有 `reviewerSessionId` 字段**（旧记录有，可用它区分新旧）；③ `~/.dsh/sessions/<workspace>/` 下**不新增**无 `session-` 前缀的目录。本工作区里 `a521318e-dc47-…`、`24bacfac-f274-…` 是 14:34/14:37 旧版子代理留下的残留，别当成新问题。
- 读会话快照：Node 的 `zlib.zstdDecompressSync` 解 `session.v3.jsonl.zstd`；**文件是多帧 zstd**，`zstdDecompressSync` 只解第一帧 —— 第一帧恰好只有 header（`agentPreset` 等），于是看起来「事件不在其中」。按魔数 `28 B5 2F FD` 切开逐帧解就能拿到全部事件（本机实测 54 帧 / 164 KB → 558 KB、118 行）。脚本要用 `.cjs`：仓库 `package.json` 是 `"type": "module"`，`.js` 会按 ESM 跑、`require` 直接报错。
- **当前生效档位看 `permission/preset` 事件，不看 header**：会话里最后一条 `{"type":"permission/preset","data":{"preset":"<id>"}}` 才是插件闸门认的档位（2026-09-15 实测本会话 seq 0 就是 `auto-approve`，插件确实被调用；header 里的 `agentPreset` 只是创建时的值）。
- **两条计数路径的真机验证（2026-09-15 19:00 前后，`session-dc9cf74f…`，「自动审批」档位）**：① 人工三次 —— 同一条带 `danger-full-access` 的 `pnpm exec vitest run tests/records.spec.js` 连续被判 deny → 用户点「允许一次」三次 → 计数 `allow=1/2/3` → 第 3 次弹「是否加入白名单」→ 确认后落盘 `command_prefix: pnpm exec vitest run tests/records.spec.js`；② 自动放行 —— `by=auto / verdict=allow` 那次同样把 `allow` 加到 1，随后两次审查改判 deny、由人工放行凑满 3 次，**混合来源照样触发**（计数只认最终结果）。想单独验证纯 auto 又不想动阈值：把白名单阈值临时降到 1，一次 auto 就立刻触发（实测通过）。
- **审查器会放行提权请求（2026-09-15 实测，值得警惕）**：同一条「`danger-full-access` 跑 vitest」命令 3 次里模型放行 1 次（`by=auto`，理由「用户已明确授权」），另 2 次按提示词判 deny —— **自动放行路径可以自动授予提权**，与「白名单绕过提权禁令」同一类；要收紧得在插件里先拦带 `sandbox_permissions` 的请求。同一次模型给的 `suggestedRule` 是更宽的前缀 `pnpm exec vitest run`，采纳后 `ruleCovers` **合并掉了两条更窄的规则**（名单 6→5 条）：即「加入白名单」可以落地一条比本次动作宽得多的规则，条件只写在选项的说明文字里。
- **策略文件在工作区之外，插件沙箱下写不了**（实测 `[sandbox: file access denied under workspace-write mode]`）；且宿主进程内存里一直持有策略文档，**任何一次审批都会把内存态整体写回文件** —— 所以在宿主运行期间手工清文件里的计数键无效（那次提权审批本身还会再加一个键）。要清只能在宿主停止时改文件；0 值计数键不影响任何行为。
- PTC 会话实测（2026-09-15 15:19，`session-05045b85…`，档位「自动审批」）：① `pnpm test` + 提权重试 → 命中**项目白名单**（`command_prefix: pnpm test`）直接放行，日志只有「策略命中」、**完全不启动审查**，记录 `decidedBy=auto` / `steps=0` / 带 `policy`；② `pnpm vitest run tests/policy.spec.js` + 提权重试（不在名单）→ 单轮审查 4 秒返回 `risk=high outcome=deny` → 转人工，用户手动拒绝。两条记录都没有 `reviewerSessionId`。
- **标准模式实测（2026-09-15 15:57–15:58，`session-0faff011…`，档位「自动审批」）**：① `pnpm test 2>&1 | Select-Object -Last 12` + 提权重试 → 命中项目白名单直接放行（`decidedBy=auto` / `latencyMs=1` / 带 `policy`、无 `route`，日志只有「策略命中」），测试真跑起来 **102 个用例全绿**；② `pnpm vitest run tests/records.spec.js` + 提权重试（不在名单）→ 单轮审查 `mode=single-shot`（15:58:19.507 开始 → 15:58:22.918 完成，`risk=high userAuthorization=medium outcome=deny`）→ 转人工，用户拒绝。**三项核对全部通过**：日志有 `mode=single-shot`、无 `reviewerSession=`、两条记录的 `callId` 是 `call_00_syW9LrsbP81rnB2W9Xfc1059` / `call_00_ziFohAoxIC0evlRUtonC1678`（**不带 `:ptc:` 后缀**），且该时刻未新增无 `session-` 前缀目录。转人工 `reason` 是两段式（调用方原文 + `\n\n模型审批意见：命令本身只是跑测试，但附带 danger-full-access 提权请求，按规则须转人工审批。`）。
- **白名单会绕过提权禁令**：同样是 `sandbox_permissions: danger-full-access`，命中白名单的 `pnpm test` 直接放行，不在名单的 `pnpm vitest …` 却被审查判 `high/deny`。名单是用户逐个确认过的强授权，这是当前设计；要收紧得改名单，或在名单匹配前先把提权请求排除（尚未做）。
- 调试时**别直接 curl 插件路由**：`http://127.0.0.1:43120/api/dsh-auto-pass/*` 对裸请求返回 `forbidden`（`dsh-client-connection` 有鉴权），要看数据就读 `~/.dsh/dsh-auto-pass/records/<slug>.json`（每个工作区一个文件、文件内按时间正序，最近的在末尾；旧版单文件是 `approvals.json`）与宿主日志。
- **哪些情况会／不会触发模型审查（判定顺序，2026-09-15 实测）**：① 黑名单命中 → 直接转人工，**不审查**；② 白名单命中 → 直接 `allowed-once`，**不审查**（15:19:12 实测：日志只有「策略命中」、没有「开始审查」）；③ 两者都没命中 → 调模型审查（15:19:40、15:28:30 两次实测）。**但前提是先产生审批请求**：审批请求只在工具调用带 `sandbox_permissions` + `justification` 时产生（`dsh-sandbox` 的 escalate 流程）；没带就没有审批，插件不可能被调用。`sandbox: workspace-write / enforcement: partial` 下 vite 的 spawn 失败是命令自己挂（crashpad「拒绝访问」），DSH 当普通命令失败返回，**连 `[sandbox: … denied]` 标记都没有**，所以插件既拦不到也无从记录——「第一次跑测试报 EPERM」属这一类，不是审查失败。
- **提权请求当前必然转人工**：`prompts/review.md` 第 12 行把「请求 danger-full-access / sudo / 写工作区外」列为一律 deny，所以不在白名单的提权调用**一定** deny → 转人工；能自动放行的只有白名单命中那条路。`dsh-sandbox` 的 `validateEscalationArgs` 实现层只校验 `sandbox_permissions` 与 `justification` 成对出现，**没有**「必须先被拒」的代码校验，但 `dsh-tool-bash` 的工具描述明确禁止预判式提权（“Never escalate speculatively”）；提权按调用生效（注释原话 `stamp onto exactly this call`），实测每次提权都要重新审批。
- **一眼看出「这次到底调没调模型」**：审批记录里**同时有 `route` + `riskLevel` + `userAuthorization` = 走过模型审查**（`route` 是本次审查路由，`userAuthorization` 由模型判定）；命中名单或 `action` 拿不到时**没有 `route`**，`latencyMs` 只有个位数毫秒、`rationale` 是插件固定文案。实例（2026-09-15）：15:19:12 白名单 `latencyMs=2`、无 `route`；15:19:44 与 15:28:36 两次 `route={provider:'commandcode',model:'deepseek/deepseek-v4.1-flash'}`、`userAuthorization=unknown|medium`、宿主日志各有一对 `开始审查/审查完成`（15:28:30.899 → 15:28:36.433）。审查期间**没有任何 UI 反馈**，之后才弹原生审批卡，所以从界面上看像「直接弹人工」。

## 客户端半与审批记录（读源码确认）

- 客户端半是 `src/client.js`，由 `package.json` 的 `exports["./client"]` + `dsh.client = { platform: 'web', inject: [...] }` 声明；载体把它变成 `/plugins/dsh-auto-pass/client.js`，文件本身必须是 `window.__ModuleLoader__.load({ id, factory })` 形式（`id` 必须是包名），factory 里用 `require('react')` 取基座 React，导出 `inject` + `apply`。`dsh.client.inject` 列的是必须**先于**本插件加载的包（这里：`dsh-api-remotes`、`dsh-client-ui-conversation`、`dsh-client-ui-sidebar-right`——后两个提供我们要用的槽）。
- **必须导出 `exports["./package.json"]`（踩过坑，代价是四次重启）**：合成器 `dsh-client-modules` 的 `locatePkgJson` 在拿不到 loader `internal.resolveSync` 时走回退分支 `createRequire(baseUrl).resolve('<pkg>/package.json')`；本机 loader 恰好走的就是这条。`exports` 一旦是受限白名单（只有 `.` 与 `./client`），该调用抛 `ERR_PACKAGE_PATH_NOT_EXPORTED`，被 `catch { return }` 吞掉 → `pkgMeta` 缓存 `null` → **本插件静默不入图：不报错、宿主日志无任何 warn、渲染器也不报 boot failed**，表现就是「各入口完全不存在」。已核对：图里 66 个 row 的第三方插件（dsh-context / dsh-todo-guard / dsh-change-review / dsh-better-sidebar / dsh-notify-me …）**全部**导出了 `./package.json`。`tests/package.spec.js` 把它钉成断言。
- 排查「为什么不入图」的判定链（可复用）：宿主日志 probe 报 `graphEntries=66 graphHas=false` 且**无** client-modules 报错 → 不是 throw（throw 会 FATAL 掉整个 clientModules 服务，graphEntries 会变 0），只能是 `resolveMeta` 静默返回 `null`；再比对渲染器缓存里的 boot 清单（`__DSH_BOOT__` 内联在首屏 HTML 里，落在 `%APPDATA%\DSH Desktop\Partitions\dsh-desktop-renderer\Cache\Cache_Data`）确认 row 真的不在图里，而不是加载后才掉队。
- **两个面板刻意分开**：对话区标签页 = 「审批设置」（**两侧**阈值输入 + 项目/全局 × 白/黑名单），右侧栏 tab = 「审批时间线」（倒序记录 + 每条可升级/降级）。`placement` 的 `tab`/`sidebar` 现在各只留一处；`all` 两处都注册；`auto` 优先右侧栏、无座位退回对话区。
- 面板数据形状：`/policy` 快照返回 `thresholds.{allow,deny}`（不再是单个 `threshold`），保存阈值用 `{ op: threshold, list, threshold }`；记录里除 `approvals` 外还有 `denials`，以及规则询问的结果 `ruleApplied` / `ruleDeclined`（时间线显示「已询问，未加入」）。
- **布局要与对话等宽（读源码确认）**：对话区面板 = `.ap-frame`（`padding:16px calc(var(--dsh-composer-side-clearance,16px) + 16px) 24px`、`align-items:center`）里的 `.ap-col`（`width:100%;max-width:var(--dsh-chat-content-width,748px)`），再往里是一张张 `.ap-card`。这两个变量由会话根元素 `._0cyzDW_root` 下发（`publishWidths` 用 ResizeObserver 写 `--dsh-conversation-column-width`，`--dsh-chat-content-width = clamp(680px, column*0.64, 920px)`）；`conversation.view` 的内容渲染在 `._0cyzDW_viewArea` 里、是该根元素的后代，所以变量能继承到。官方插件 `dsh-client-ui-approval` / `dsh-client-ui-user-questions` 用的是同一套写法（照抄它们的对齐方式，别自己拍宽度）。
- **设置页卡片必须渲染 `<li>`（踩过坑）**：`settings.plugin.item` 的宿主容器是 `ul.JMEyFa_cards`（外层 section `max-width:760px`），**整张卡片由插件自己拥有**。早期用 `<div>` + 内联 style，卡片样式一条都没生效，表现就是「设置里只有一段裸文字」。现在用 `card('li', …)` 复刻内置插件卡 `.TKtcza_card` 的外观（`.5px` 描边 `--dsw-alias-border-l4`、底色 `--dsw-alias-bg-layer-3`、16px 圆角、标题 15px/600 + 13px 说明）。
- **时间线行内展示**：折叠态除时间/工具/结论外还有两行——**审批意见**（`record.rationale`，CSS 单行截断）与合并后的**标签行** `TagRow`（`.ap-hitRow` / `.ap-hitText`，chip + 说明文字）。
- **时间线快捷筛选（2026-09-15 新增，用户要求）**：面板顶部 `.ap-filters` 里是五个 `.ap-chip`（`全部` + `白名单` / `黑名单` / `自动` / `人工`），每个 chip 后面跟 `.ap-chipCount`（**当前范围**——本次会话或全部会话——内的条数，为 0 也显示）。匹配口径：`allow` / `deny` 看 `record.policy.list`，`auto` / `human` 看 `decidedByOf(record)`（老记录按 verdict/outcome 兜底推断），但**命中名单的记录只算白/黑名单两类**（`policy.list` 存在时直接不算 auto / human）——`auto` = 模型自动放行、`human` = 你最终拍板，都不含被名单拦下的那些（2026-09-15 用户改口径：原先两个维度重叠，命中白名单那次会同时算「自动」）。四个键因此互不重叠、条数相加 = 总数。**多选叠加、并集**（`filters.some(...)`），一个都没点亮 = 全部；点 `全部` 清空（`data-on="1"` 只落在选中的 chip 上，`.ap-chip[data-on="1"]` 用 business 色）。筛空时显示 `t.filterEmpty`（「没有符合筛选条件的记录」），与「还没有审批记录」区分；标题栏的 `auto/total` 仍是**未过滤**的整体概览。筛选状态是面板内部 state（切会话、刷新即重置），不进宿主设置。测试在 `tests/client.spec.js`：按 `data-filter` 属性找 chip（chip 里除文字还有计数 span，`findButton` 那套「只有一个字符串子节点」不适用）。
- **审批记录带「第几轮第几步」（2026-09-15 新增，用户要求）**：`buildRecord` 写 `turn` 与 `step`（来自本次动作对应的 `tool/call`；ptc 档位由 `exactAction` 从父调用补齐），时间线折叠态在工具名左边用 `.ap-turnStep` 小字显示「第 N 轮 · 第 M 步」，展开详情里也有一行（`t.turnStep`）；老记录没有这两个字段时整行/整段不显示。**注意别与通知里的「0 步」混淆**：那个是审查调查步数（单轮审查恒为 0）。
- **「全部会话」下每条记录底部有小字会话名**：`sessionNameOf(sessionId)` 读客户端 `ctx.get('sessions')` 的 `list.getSnapshot().byId[id].displayTitle`（与 DSH 左侧会话列表同一份投影，源码里 `displayTitleOf` = durable title → cwd basename → 原始 id；dsh-context 也走 `ctx.get('sessions')` + `list.getSnapshot()`）。会话不在当前列表快照里（历史会话）时退回 id 短形式：**先去掉 `session-` 前缀再取前 8 位**——真机 id 形如 `session-<uuid>`，直接截前 8 位会全是 `session-`，等于没区分度。这一行只在 `scope === 'all'` 时渲染（`RecordRow` 的 `showSession` prop），位置在 `<li>` 最末（展开时在详情之后），`title` 挂完整 sessionId，样式 `.ap-session`（11px、单行截断、靠右对齐）。不额外订阅会话列表：快照晚到靠时间线 3 秒轮询的 setState 重渲染补上。
- **折叠态标签只有一行（用户明确要求）**：`TagRow` 把「命中名单」与「决策来源」合并——chip = 「名单·自动/人工」（没命中名单时只剩 自动/人工），说明文字 = 命中规则信息（`作用域 · 规则标签`），没命中时是决策含义。含义与 tooltip：**自动 = 模型自动审批，人工 = 人工审批通过/拒绝**（或无人应答/已取消）。示例：`[白名单·自动] 本项目 · 放行 pnpm test 全量测试…` / `[黑名单·人工] 本项目 · bash · rm -rf` / `[自动] 模型自动审批` / `[人工] 人工审批 · 已拒绝`。
- **标签的两个数据来源**：① `decidedBy = 'auto' | 'human'`，由宿主 `decisionSource(settled, decision)` 写入（插件自己放行 = auto；转人工后由人决定 = human），老记录缺字段时客户端按 `verdict`/`outcome` 兜底推断；② 命中名单取 `record.policy`（`describeHit` 已带上 `source`，并可在客户端按 `ruleId` 去策略快照反查 `ruleSourceOf()`，所以时间线挂载时要拉一次 `/policy`）。**规则自身的来源（手动/模型/记忆）不再挤进时间线 chip**，仍由「审批设置」面板的规则列表显示。注入通知的一行摘要用 `[自动]` / `[人工]` 前缀（纯文本没有 chip 形态）。
- 项目目录的客户端来源：优先宿主 `sessions` 服务的 `list.getSnapshot().byId[sessionId].cwd`（照 dsh-context 的 `workspaceOf` 写法），拿不到就用本会话最新一条审批记录里的 `cwd`。**不要**把 `sessions` 写进 `exports.inject`——它只是可选探测，注入一个不存在的服务会让插件挂起。
- 两处放置位置（参照 dsh-context 0.52.1 的实现）：
  - 对话标签页：`ctx.slots.inject('conversation.view', () => ctx.slots.register({ name:'conversation.view', id, order, label }, Cmp))`，出现位置就在「对话 / 轨迹」旁边。
  - 右侧栏 tab：`ctx.inject(['sidebarRightTabs'], raw => ...)`（**延迟注入，座位按契约可选**）→ `raw.sidebarRightTabs.register({ id, kind, title, guide:[{ order, title, description, icon }] })` + 正文 `raw.slots.inject('sidebar.right.pane.tab', …)` + **标题** `raw.slots.inject('sidebar.right.pane.tab.title', …)`（`key` 都等于 tab id）。**标题席位不给就只有文字、没有图标**——官方 chip 是「图标 + 标题」，dsh-context 就是这么做的（`makeContextTabTitle`）；我们照它把 `LogGlyph` + 文案注册进标题席位。`id` 同时是正文席位的 key；tab 一次都没打开过时它只出现在引导页/加号菜单里。
  - `slots.inject` 返回**幂等 disposer（函数）**；cordis 的 `ctx.inject` 返回带 `.dispose()` 的 handle，两者的卸载方式不同。
- 面板数据来自 host 的 `GET /api/dsh-auto-pass/log?session=&limit=`（记录，倒序）、`/api/dsh-auto-pass/policy?cwd=`（阈值 + 两级名单）、`/api/dsh-auto-pass/config`（`settings.{placement,notice,denyDirect,autoOpenTimeline,askRejectReason}` + `writable`）；客户端每 3 秒轮询一次。
- **设置页卡片与「审批设置」面板共用同一张卡片**：都渲染 `SettingsCard`（设置页传 `tag:'li'` —— 宿主容器是 `ul`；对话面板传 `tag:'section'`），内容 = 放置位置（`PlacementControl` 分段按钮）+ 四个行为开关（`SwitchRow` + `behaviorSwitchSpecs()`：注入审批结果 / 黑名单直接拒绝 / 自动打开审批时间线 / 拒绝后追问理由）。**开关是真正的 switch**：`label.ap-switch` 里放原生 `input[type=checkbox].ap-switchInput`（键盘可用、读屏可识别）+ 轨道 `span.ap-switchTrack`（`:checked` 变色、滑块 `translateX(14px)`）+ 开/关文字；标题仍在同一行的 `.ap-fieldKey` 里（最初把标题塞进 switch 内导致「开关没有名字」，被 `tests/client.spec.js` 的渲染断言抓住）。
- **`autoOpenTimeline`：只要触发审批、且审批时间线没打开，就自动展开右侧栏时间线（2026-09-15 按用户要求改口径，默认开）**：客户端在 `applyInner` 末尾挂一个**与面板无关**的观察器 `watchApprovals()`（`setInterval` 每 `POLL_MS`=3s，另外启动时立刻跑一次；放在 `remount()` 之后，此刻右侧栏 tab 已注册）：读 `ctx.get('sessions').list.getSnapshot().current` 拿当前会话 → `GET /log?session=<id>&limit=1` 看最新一条记录 → `approvalWatch.seen` 里没有该会话就是**本轮页面会话第一次看到它，只记基线**（刷新页面后堆着的历史记录不该把时间线弹开），此后最新记录的 id 一变就说明刚发生了审批 → `maybeAutoOpenTimeline()`。
  三重门槛：设置开着 → `mountState.sidebar` 已注册（placement=tab 时不注册，也就没有可自动打开的时间线）→ `mountState.timelineShown !== true`。`timelineShown` 由时间线面板自己维护（取 `useTabInfo().tab.visible`，**收起侧栏、切到别的 tab 都算没打开**；`useTabInfo` 缺失的单测环境按「打开」处理），面板卸载时归 false——所以判定规则是「没打开就打开、已经显示在眼前就不抢焦点」。真正展开靠官方服务 `ctx.get('sidebarRight').openTab(SIDEBAR_KIND)`（`dsh-client-ui-sidebar-right` 的 `openContent` 内部会 `planSetExpanded(true)`，不用自己拼 layout 调用）；服务缺失或抛错时**只重试一次**（`AUTO_OPEN_RETRY_MS = 150`），仍失败只告警、下次审批再试。**不要用 `ctx.get('sidebarRightTabs')` 探测服务可用性**：那是 tab 类型注册表（`register`），没有 `openTab`；导航面是另一个服务名 `sidebarRight`（两者都由同一个客户端插件 `provide`）。成功/失败都会打 `beacon('auto-open-timeline')`。
  **旧实现为什么等于没生效（2026-09-15 查宿主日志确认，改动前）**：探针写在审批设置面板 `resolve()` 的「拿不到项目目录」分支里，而 `workspaceOf(sessionId)` 通常能拿到 cwd → 那段代码根本不执行；唯一会走到的是页面加载瞬间（会话列表快照还没到），时间线面板又只在 tab 被打开时才挂载。宿主日志里 `client beacon stage=auto-open-timeline` **只出现在页面刷新后的头几十毫秒**，正常使用中触发审批时一次都没有。现在测试见 `tests/client.spec.js`「审批触发后自动打开右侧栏时间线」（假时钟驱动：首次基线不弹 → 新记录弹 → 面板显示时不抢焦点 → 面板卸载后再弹）。
- **`askRejectReason`：人工拒绝后追问一句拒绝理由（2026-09-15 新增，默认开）**：宿主 `finish()` 在结论确定后判断「`askRejectReason && settled === 'rejected' && decisionSource(settled, decision) === 'human'`」，命中就把它**入队**（`enqueueAsk('追问拒绝理由', …)`，见下面「两张问题卡串行」）——**旁路、绝不 await**：结果那一行照旧立刻注入，用户补了理由再由 `injectReasonNotice()` 注入**第二行**（用户选定的语义是「先给结果、理由到了再补一行」，这轮工具结果不被拖住）。追问走 `ctx.userQuestions.ask({ questions: [{ id: 'dsh-auto-pass:reject-reason', … }], agent: request.agent })`：**第一项默认「采用模型意见：<本次审查的 rationale>」**（审查失败 / 命中名单这类没有模型意见的路径只剩「不留言」一项），用户也可以直接在原生问题卡的「输入你的答案」里写自己的理由——**原生卡在有自定义答案时会把 `selected` 清空**，所以解析必须**自由文本优先**；选「不留言」或认不出答复一律不注入。理由同时回写记录 `rejectReason`（时间线展开详情多一行「人工拒绝理由」）。**只受 `askRejectReason` 控制，不被 `notice` 连带**（2026-09-15 用户明确要求改）：notice 关掉时结果行不注入，但追问照发、理由照作为第二行注入——所以「notice 关掉 = 上下文里一行都没有」不再严格成立，那一行是用户自己写的理由。**真机复验（2026-09-15 19:19，重启后，`notice=false`）**：拒绝后宿主日志只有 `已注入人工拒绝理由 chars=47`（**没有**结果行），上下文里只多出 `[人工] 人工拒绝理由：写入工作区外用户目录且申请 danger-full-access…` 一行，记录 `rejectReason` 同步回写——解耦生效。问不到人（子 Agent / 无应答器 / 调用已中止）只记日志，审批结论与结果通知都不受影响。测试在 `tests/auto-approve.spec.js`：模型意见选项 + 第二行注入 + 记录回写、两条不追问路径（`askRejectReason` 关 / 自动放行）、notice 关掉不连带（只少结果行）、自由文本 / 不留言 / 无服务、blacklist-reject 不追问、串行队列。
- **两张问题卡串行（2026-09-15 用户要求）**：`finish()` 里的「拒绝理由追问」与「达阈值后的规则确认」共用 handler 作用域内的一个串行队列 `enqueueAsk(label, task)`（`let askQueue = Promise.resolve()`；每环 `askQueue.then(run, run)` 后再挂一个只记日志的 catch，所以链**永不 reject**，前一环失败不阻塞后一环）。顺序固定为**理由追问在前、规则确认在后**，两张卡不会同时抢占输入框；队列依旧是旁路——不 await、不拖住工具结果。它与 `pendingSuggestions`（同一签名同一名单同时只挂一个问题）是两件事：后者防重复，前者防并发。测试见 `tests/auto-approve.spec.js`「追问理由与规则确认串行」：把第一张卡的 promise 挂起不答，断言第二张卡的 `ask` 根本没发出，放行后才发出。
- **`decisionSource` 修正（2026-09-15）**：旧写法 `pluginDecided && settled === 'allowed-once'` 会把「黑名单直接拒绝」（settled 是 `rejected`）算成 `human`，与该函数自己的注释（插件自己决定 = auto）相反。现在 `verdict === 'blacklist-reject'` 直接返回 `'auto'`，客户端 `decidedByOf` 的兜底分支同步——黑名单直接拒绝的折叠 chip 因此是「黑名单·自动」、注入标签是 `[黑名单·自动]`（这次判断确实没有人参与）。
- 放置策略 `placement: auto | tab | sidebar | all`，默认 **all**（与 dsh-context 一致：对话标签页 + 右侧栏 tab 都注册）；`auto` 表示 `ctx.get('sidebarRightTabs')` 存在就挂右侧栏，否则退回对话标签页。
- **设置页卡片的渲染条件（踩过坑）**：设置 → 插件 只为**宿主侧 `settings.register(命名空间, schema)` 注册过的命名空间**渲染卡片，并按 `entryKey = 命名空间` 派发 `settings.plugin.item`。只注册客户端卡片不会显示任何东西。本插件 host 半用动态 `import('@deepseek-ai/schemastery')`（软依赖，失败只丢卡片）注册命名空间 `dsh-auto-pass`，客户端卡片 key 与它同名。
- **界面偏好统一走设置命名空间（`GET/POST /api/dsh-auto-pass/config`）**：`placement`（面板位置）、`notice`（是否把审批结果注入模型上下文，默认 true）、`denyDirect`（命中黑名单直接拒绝，默认 false）、`autoOpenTimeline`（只要触发审批、且时间线没打开就自动展开它，默认 true）、`askRejectReason`（人工拒绝后追问一句拒绝理由，默认 true）。宿主侧 schema 在 `installSettings()` 里注册（`z.boolean().default(...)`，所以 `settings.get()` 一定返回全部五个键），读取一律走 `readSetting(ctx, key)` → `effectivePlacement/effectiveNotice/effectiveDenyDirect`（设置值优先、读不到回落到 config，再回落到 DEFAULTS）；写入口是 `updateConfig()`，只接受 `CONFIG_SETTINGS` 白名单里的键（`settings.update` 是 patch 语义，未提到的键保持原值），非法值 400、没有可写 settings 时 503。**读设置在每次审批时发生**，所以设置页一改就生效、不需要重启。**坑（2026-09-15 真机踩到，已修）**：这三个开关原先在 `createAutoApprovalHandler()` 里**构造期**求值，而 `apply()` 是先建 handler（`ctx.on(...)` 那一行）再 `installSettings()`，此刻 `ctx.get('settings')` 还没就绪 → `readSetting` 静默返回 undefined → 三个开关**全部退回 DEFAULTS**。表现：设置页把「黑名单直接拒绝」打开，命中黑名单仍然 `defer` 转人工；把「注入审批结果」关掉也照旧注入。修法是把 `effectiveNotice/effectiveDenyDirect/effectiveAskRejectReason` 移进返回的 handler 体内（每次审批重读）；回归用例 `tests/auto-approve.spec.js`「行为开关每次审批重新读设置」先建 handler、再翻转 settings 并断言 `next` 不被调用——把代码改回构造期读会立刻变红（已实测）。**真机复验（2026-09-15 19:12，重启宿主后）**：同一条 signature 黑名单命令——修复前 `verdict=defer / by=human / latencyMs=25623`（弹卡等用户点），修复后 `verdict=blacklist-reject / by=auto / latencyMs=3 / route=none`，宿主日志出现 `黑名单直接拒绝（设置已开启）`，注入行是 `[黑名单·自动] 自动审批 已直接拒绝 pwsh（命中黑名单） · … · 最终结果：已拒绝`，且**不追问拒绝理由**（auto 决策不触发）。客户端侧所有偏好都收在一个 `runtimeStore` 里（placement/notice/denyDirect/autoOpenTimeline/askRejectReason + writable），旧的 `placementStore` 只剩 `value`/`subscribe` 薄壳给挂载逻辑用，行为开关的变更不会触发重新挂载。**布尔开关一律走 `effectiveFlag(ctx, config, key)`**：设置值 → config 值 → DEFAULTS，只认真正的 boolean（写坏的设置文件或字符串 `"false"` 会往下一层回落，而不是让开关静默失效）。
- 记录仓库在 `src/records.js`：内存列表 + JSON 原子落盘（`.tmp` + rename），**按工作区分文件**——`$DSH_HOME/dsh-auto-pass/records/<slug>.json`（`workspaceSlug(cwd)`：转小写、非 `[a-z0-9]` 折叠成 `-`、去首尾 `-`；超 48 字符截断并补 8 位 sha1；没有 cwd 的进 `unknown.json`）。`maxRecords`（默认 1000）是**每个工作区各算**，一个工作区刷屏不会挤掉别的工作区的历史；`list({session})` 把各文件合并成一条时间线倒序返回（只有 `session` 一个过滤维度，面板「全部会话」= 不带参数 = 所有工作区混排）。**单文件形态只在显式配置 `logFile` 时启用**（`createRecordStore({ file })`，调试与对照用；`records.dir` / `records.file` 二者只有一个有值，`/log` 与启动日志按此回报）。启动时若发现旧单文件 `dirname(dir)/approvals.json`（2026-09-15 晚之前的形态）就按工作区拆开：**全部写成功才删原文件**，中途失败保留原文件下次再试；装载路径按 id 去重（`recordKeyOf`：字符串 id 与数字 id 都认），因此旧文件被旧版本进程写回、或迁移跑过两次都不会重复刷屏（真机实测：148 条 + 含重复的 285 条 → 148 条唯一记录 / 3 个工作区文件）。`createAutoApprovalHandler(ctx, config, records)` 的第三个参数默认 `noopRecordStore`；`finish()` 里对 `records.add` 做 try/catch，**写记录失败绝不影响审批结论**。

## 部署（本机 desktop profile）

- 依赖形态：`C:\Users\czy\.dsh\profiles\desktop\package.json` 里写 `"dsh-auto-pass": "link:D:/work/github/dsh-auto"`，`dsh.profile.bundles` 末位是 `dsh-auto-pass`；`node_modules\dsh-auto-pass` 是指向本仓库的**符号链接**（等价于 pnpm 对 `link:` 依赖的物化结果，参照同 profile 的 `dsh-change-review`）。`pnpm-lock.yaml` 的 importer 段已同步为 link 形式，旧的 `dsh-auto@github:...` 条目已删除。
- `dsh plugin <args>`（= 在 profile 目录转发 pnpm）在本机**当前不可用**：不加 `-w` 报 `ERR_PNPM_ADDING_TO_ROOT`（profile 有 `pnpm-workspace.yaml`），加了 `-w` 报 `ERR_PNPM_VIRTUAL_STORE_DIR_MAX_LENGTH_DIFF`（`node_modules/.modules.yaml` 记录 `virtualStoreDirMaxLength: 60`，而 shim 的 pnpm 9.15.9 默认值不同）。要重建依赖需显式带上该配置值（未验证）。
- **权限档位的图标是 DSH 客户端硬编码的，第 4 个档位拿不到（2026-09-15 查证）**：输入框上方档位 chip/菜单里的图标来自 `@deepseek-ai/dsh-client-ui-conversation` 的 `permissionGlyphs` —— 一个只含 `read-only` / `workspace-write` / `danger-full-access` 三个 **id** 的 `Map`（注释原话：host-configured names outside the design set get none）；设置页的权限菜单（`dsh-client-ui-permission-presets`）更只有纯文字 `{id,label}`。宿主 `presets.<id>` 的 schema 只有 `{sandbox, approval, name, description}`，**没有图标位**（`name` 里的字符是唯一的可视抓手，彩色 emoji 又太扎眼），所以现在由插件自己画：`src/client.js` 的 `installPresetIcon()` 用 MutationObserver（**childList + characterData**：切换档位时 label 文本是在同一个 span 上改写的，只观察节点增删永远补不上图标）在档位 chip 与下拉菜单项里找文字精确等于档位名的**纯文字** `<span>`（`isPresetLabelNode` 只认 textContent + 无子元素，不依赖 DSH 的哈希类名），先 `applyPresetIconVars` 写内联 `--ap-glyph-*` 变量、再挂 `ap-presetGlyph`；选择器是 `[role=menu] span, [role=menuitem] span, [aria-haspopup=menu] span, button span, [aria-label*=档位名] span` —— **composer 的 trigger 没有 `aria-haspopup`**（菜单原语只渲染 anchor、不补属性），只按 aria 属性选会漏掉 chip；反正按文字精确过滤，选择器放宽不会误伤，同一帧内多次变动用 rAF 合并。**几何必须走变量，别写成「两个 class 比层叠」（2026-09-15 实测踩坑）**：第一版把菜单态写成 `.ap-presetGlyphMenu::before{width:16px;margin-right:8px;…}`，源码、注入的 `<style>`、以及渲染器缓存里那份 served bundle（`%APPDATA%\DSH Desktop\Partitions\dsh-desktop-renderer\Cache\Cache_Data`，可以直接读出来核对）**三处都确认有这条规则且顺序在后**，但用户在真机截图里量出来的仍是 chip 那套（图标 ink 13×14px、文字比内置项早 5px 起）—— 也就是那条规则没参与。改成**内联自定义属性**（`--ap-glyph-box/-icon/-gap/-color` 由 JS 直接写在 span 上，CSS 只有一条规则读它们）后，几何不再有任何层叠余地。顺带记下内置的真实尺寸（用截图逐列量得）：菜单项 `.itemIcon` 盒 16px、里面的 svg 带 1.31831 描边 → 实际 ink 15×16px、与 label 间距 8px、颜色 `--dsw-alias-label-tertiary`；chip `.triggerIcon svg` 14px、间距 4px、颜色跟文字。**对齐交给 flex，不要用 `vertical-align` 手调（2026-09-15 又踩一次）**：`-3px` / `-2px` / `middle` 都试过，真机上始终差约 2px（用户反馈「图标与文字不对齐」）；现在 `.ap-presetGlyph{display:inline-flex;align-items:center;gap:var(--ap-glyph-gap,8px)}` —— 图标与文字成为 flex 项、由 `align-items:center` 居中，与内置 `.itemIcon` / `.triggerIcon` 同一套机制（间距也从 `margin-right` 改成 `gap`）。**怎么自查**：`report()` 会在每处宿主第一次打标记时把浏览器算出来的 `::before` 宽高 / 图标色、以及 span 自身的 `gap` `display` `line-height` 经 `GET /api/dsh-auto-pass/beacon?stage=preset-icon&detail=…` 写进宿主日志（`%APPDATA%\DSH Desktop\logs\host\dsh-<date>.log`），「看着不对」时先读日志核对，别靠猜。`name` 因此保持纯中文 `自动审批`（非 kebab-case 的 name 会被两处客户端原样透传）。**不要为了图标去改 id**：`src/index.js` 的闸门是 `selectedPermissionPreset(session) !== 'auto-approve'`，且三个内置 id 各有语义不可顶替。
- 本机默认 `reviewerProvider: deepseek-official` 无凭证，必须由 profile 的 `cordis.patch.yml` 用 `- id: dsh-auto-pass` 覆盖审查模型；profile 层按 id 覆盖会**整体替换** config，所以要重述全部键。
- 改插件代码、`cordis.patch.yml` 或策略后必须重启 DSH Desktop 才生效（客户端半也要重启，HMR 只在 dev:web 下生效）。
- profile 的 `cordis.patch.yml` 覆盖里没有新键（`maxRecords`/`placement`/`autoApproveAfter`/`policyFile`）也无需改：profile 按 id 覆盖时未列出的键回落到代码默认值。
- **仓库随包的 `cordis.patch.yml` 已同步为当前键集（2026-09-15 清理）**：config 现在只有真实被读的键——`language` / `reviewerProvider` / `reviewerModel` / `reviewerReasoningEffort` / `timeoutMs` / `maxEvidenceChars` / `maxActionChars` / `maxOutputTokens` / `logFile` / `maxRecords` / `autoApproveAfter` / `autoDenyAfter` / `policyFile` / `placement` / `notice` / `denyDirect` / `autoOpenTimeline` / `askRejectReason`。旧的 `maxInvestigationSteps` / `maxMessageTranscriptTokens` / `maxToolTranscriptTokens` / `maxMessageEntryTokens` / `maxToolEntryTokens` / `maxSystemInstructionTokens` / `maxAgentInstructionTokens` / `maxRecentNonUserEntries` 已从仓库文件里删除（全仓 grep 确认 `src` 里没有任何读取处）。两个 README 的 YAML 块**故意只列「适合写在配置文件里」的子集**（reviewer 路由三件套 + `language` + `timeoutMs` + `maxEvidenceChars`/`maxActionChars`/`maxOutputTokens` + `logFile`/`maxRecords`/`policyFile`），界面偏好（`placement`/`notice`/`denyDirect`/`autoOpenTimeline`/`askRejectReason`）与两侧阈值（`autoApproveAfter`/`autoDenyAfter`）改在正文里讲「可在界面改」。YAML 块后面紧跟 `### 参数说明`（英文 `### Key reference`）**逐键解释**——每个键的含义、默认值、类型/成对校验口径（数值键必须正整数、`logFile`/`policyFile` 必须字符串、reviewer 两键必须成对）都写在那一份列表里；**改键时这份列表与 `cordis.patch.yml` 一起改**。改键时以 `cordis.patch.yml` 为准：README 的子集允许少于它，但**不得出现它没有的键**。
- **文档截图（2026-09-16 更新）**：两份 README 的开头「截图」一节是**同一组 3 张中文界面图**，中英 README 逐张复用同一文件：`docs/images/auto-approve-permission.zh.png`（权限菜单里选中「自动审批」）、`auto-approve-policy.zh.png`（「审批设置」面板：两侧阈值 + 全局/项目两级的白/黑名单）、`auto-approve-timeline.zh.png`（「审批时间线」面板：记录 + 快捷筛选，含「全部 4 / 白名单 4」这类计数）。已被取代或过期的 `auto-approve-permission.en.jpg`（英文界面档位图，用户 2026-09-16 要求中英统一）、`auto-approve-permission.zh.jpg`、`auto-approve-allowed.png`（子代理时代的「上下文注入 · Reviewer 会话 / 调查步骤 1」卡片）**都已删除**，别再引用。
- **单轮审查后 profile 里那几个旧键（`maxInvestigationSteps` / `maxMessageTranscriptTokens` / `maxToolTranscriptTokens` / `maxMessageEntryTokens` / `maxToolEntryTokens` / `maxSystemInstructionTokens` / `maxAgentInstructionTokens` / `maxRecentNonUserEntries`）已经是惰性的**：`resolveConfig` 不再读它们，但也不会因为多出来就报错（内部是 `{...DEFAULTS, ...config}`），所以不必急着去改 profile；要精简就删掉那几行。新增可调的是 `maxEvidenceChars`（默认 400）。
- 策略文件不在 profile 里，运行时才创建：全局 `C:\Users\czy\.dsh\dsh-auto-pass\policy.json`、项目 `<会话 cwd>\.dsh-auto-pass\policy.json`。调试时可以删掉全局文件让阈值与计数归零（项目名单会一起消失）。
- 回滚来源：`%APPDATA%\DSH Desktop\health-snapshots\<hash>\slot-N\` 保存了 profile 的副本（含 `pnpm-lock.yaml`、`package.json`）。

## 工具坑（本机实测）

- `read` 单次调用有上限：`limit` 最大 2000 行，且会被输出预算提前截断（96 KB 的 `pnpm-lock.yaml` 一次只返回约 1130 行）。**大文件必须按 600 行左右分块读并拼接**，否则写回会静默丢内容。
